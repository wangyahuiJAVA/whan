server:
  port: 8186
  servlet:
    context-path: /auth

spring:
  profiles:
    active: dev

mybatis-plus:
  #  configuration:
  #    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/**/*.xml
